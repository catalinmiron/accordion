import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
  safeArea: {
    flex: 1,
  },
});

const Colors = {
  // solid colors
  ebony: '#090F17',
  osloGray: '#87888A',
  shark: '#272C33',
  outerSpace: '#31373E',
};

export { Styles, Colors };
